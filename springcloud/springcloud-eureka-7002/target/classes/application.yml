server:
  port: 7002

eureka:
  instance:
    hostname: localhost2
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
     # defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
        defaultZone: http://192.168.177.1:7001/eureka/